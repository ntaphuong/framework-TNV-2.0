package pageObjects.users.portal;

import commons.BasePage;
import org.openqa.selenium.WebDriver;

public class UserAboutMePO extends BasePage {
    private WebDriver driver;
    public UserAboutMePO(WebDriver driver) {
        this.driver = driver;
    }
}
